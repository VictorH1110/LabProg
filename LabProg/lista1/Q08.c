#include <stdio.h>

int main() {
    int x, quad;
    printf("Digite um numero inteiro: ");
    scanf("%d", &x);
    quad = x * x;
    printf("%d ao quadrado: %d\n", x, quad);
    return 0;
}
